            if self.foo.is_none() {
                self.foo = other.foo;
            }
            if self.bar.is_none() {
                self.bar = other.bar;
            }
            if self.baz.is_none() {
                self.baz = other.baz;
            }
            if self.verbose.is_none() {
                self.verbose = other.verbose;
            }
            if self.fast.is_none() {
                self.fast = other.fast;
            }
