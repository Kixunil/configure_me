            if other.d.is_some() {
                self.d = other.d;
            }
            if other.e.is_some() {
                self.e = other.e;
            }
            if other.a.is_some() {
                self.a = other.a;
            }
            if other.b.is_some() {
                self.b = other.b;
            }
            if other.c.is_some() {
                self.c = other.c;
            }
            if other.foo_bar.is_some() {
                self.foo_bar = other.foo_bar;
            }
